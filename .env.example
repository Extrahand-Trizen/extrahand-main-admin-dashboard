# Local development — gitignored (.env is never committed).
# GitHub Actions does NOT use this file — set Variable NEXT_PUBLIC_API_URL in the repo instead.

NEXT_PUBLIC_API_URL=http://localhost:4000

# Production example (no trailing slash):
# NEXT_PUBLIC_API_URL=https://extrahand-main-admin-server.backend.extrahand.in
